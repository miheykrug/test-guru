<% if @question.errors.present? %>
  <div>
    <h3><%= pluralize(@question.errors.count, 'error') %> detected</h3>

    <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
