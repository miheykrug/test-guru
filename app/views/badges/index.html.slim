h1 = t('.header')

table.table.table-striped
  thead.thead-dark
    tr
      th = Badge.human_attribute_name('file_name')
      th = Badge.human_attribute_name('title')
      th = t('.action')
  = render @badges
