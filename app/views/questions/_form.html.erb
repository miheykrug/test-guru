<h1><%= question_header %></h1>
<%= form_for [@test, @question], local: true do |f| %>
  <%= render 'shared/errors' %>
  <p>
    <%= f.label :body, 'Вопрос' %>
    <%= f.text_field :body, id: :question_body %>
  </p>
  <p><%= f.submit %></p>
<% end %>
<%= link_to 'Назад', test_path(@test) %>
